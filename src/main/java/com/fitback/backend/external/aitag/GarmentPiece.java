package com.fitback.backend.external.aitag;

/**
 * AI model result grouping. This is intentionally separate from the tag master taxonomy's
 * {@code TagTargetClothing} applicability contract.
 */
public enum GarmentPiece {
    TOP,
    BOTTOM,
    SHOES
}
